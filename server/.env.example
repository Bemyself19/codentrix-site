# SMTP configuration for sending contact emails
# Replace with your SMTP provider credentials
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Email envelope
CONTACT_FROM="Codentrix Website" <no-reply@example.com>
CONTACT_TO=contact@codentrix.example.com

# Server port (optional)
PORT=4000
